class Solution {
    public boolean isAnagram(String s, String t) {
        int[] cnt=new int[26];
        for(char c:s.toCharArray()){
            cnt[c-97]++;
        }
        for(char c:t.toCharArray()){
            cnt[c-97]--;
        }
        for(int i:cnt){
            if(i!=0){
                return false;
            }
        }
        return true;
    }
}