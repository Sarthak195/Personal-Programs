class Solution {
    public boolean divideArray(int[] nums) {
        Arrays.sort(nums);
        int flag=0;
        for(int i=0;i<nums.length-1;i+=2){
            if(nums[i]==nums[i+1]){
                flag=1;
            }else{return false;}
        }
        return flag==0?false:true;
    }
}