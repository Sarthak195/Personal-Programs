class Solution {
    public int maxDepth(String s) {
        int n=0,max=0;
        for(char c:s.toCharArray()){
            if(c=='('){
                n++;
                max=Math.max(max,n);
            }else if(c==')'){
                n--;
            }
        }
        return max;
    }
}