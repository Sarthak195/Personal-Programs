class Solution {
    public int countSegments(String s) {
        int cnt=0;
        int n=s.length();
        for(int i=0;i<n;i++){
            if(s.charAt(i)!=' '&&(i==0||s.charAt(i-1)==' ')){
                cnt++;
            }
        }
        return cnt;
    }
}