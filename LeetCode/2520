class Solution {
    public int countDigits(int num) {
        int res=0;
        int a=num;
        int temp=0;
        while(a>0){
            temp=a%10;
            a/=10;
            if(num%temp==0){
                res++;
            }
        }
        return res;
    }
}