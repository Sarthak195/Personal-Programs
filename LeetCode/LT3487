//Incomplete code
class Solution {
    public int maxSum(int[] nums) {
        Arrays.sort(nums);
        int sum=nums[nums.length-1];
        for(int i=nums.length-2;i>=0;i--){
            if(nums[i]>0){
                sum+=nums[i];
            }
        }
        return sum;
    }
}